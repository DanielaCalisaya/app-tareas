/* Tiene ese formato, asi next.js sabe que eso es lo que va a ir cambiando */
/* Cuando se visite la página id devolverá la misma página con el TaskFormPage */
import TaskFormPage from '../new' 

export default TaskFormPage;